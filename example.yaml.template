# Example metadata schema for CVlization examples
# Based on maintain_oct17.md specification

name: example-name                    # Short kebab-case name
capability: category/subcategory      # perception/ocr_and_layout, generative/llm, etc.
modalities: [vision, text, audio]     # Input/output modalities
datasets: [dataset1, dataset2]        # Datasets used (or ["synthetic"] for generated data)
stability: stable|beta|experimental   # Maturity level
resources:
  gpu: 1                              # Number of GPUs required (0 for CPU-only)
  vram_gb: 12                         # VRAM requirement in GB
  disk_gb: 10                         # Disk space for datasets/models
presets: [train, eval, serve]         # Available workflows
tags: [tag1, tag2, tag3]             # Searchable tags (framework, model type, etc.)
tasks: [task1, task2]                # ML tasks (text_generation, object_detection, etc.)
frameworks: [pytorch, tensorflow]     # ML frameworks used
description: Short one-line description of what this example does
