Use the lsp tool to rename the function add_numbers in src/lib.py to sum_values. The project root has a pyproject.toml so pylsp should start. Propagate the rename to all callers (src/main.py and src/util.py) as part of the same rename via the LSP rename operation (action='rename', apply=true). Do not use Write or Edit -- the rename should go through the language server's workspace edit.
