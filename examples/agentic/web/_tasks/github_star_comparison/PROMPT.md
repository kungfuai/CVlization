Compare the GitHub star counts of two open-source repositories. First, go to https://github.com/torvalds/linux and read the star count displayed near the top of the repo page (next to the 'Star' button). Then navigate to https://github.com/microsoft/typescript and read its star count the same way. Compare the two numbers and decide which repository has more stars. Reply with only the winning repository's owner/name on a single line, in the form 'owner/repo' (for example 'torvalds/linux' or 'microsoft/typescript'). No commentary, no numbers, no formatting.
