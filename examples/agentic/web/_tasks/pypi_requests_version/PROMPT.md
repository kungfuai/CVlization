Open https://pypi.org. Use the search box (look for a search input near the top of the page) to search for the package 'requests'. From the search results, click the link for the 'requests' package (the HTTP library by Kenneth Reitz; it should be the top result). On the package page, find the latest version number, which is shown near the top of the page (usually formatted like '2.x.y'). Reply with only that version number on a single line, in the form major.minor.patch (e.g. '2.32.5'), no commentary, no 'v' prefix, no quotes.
