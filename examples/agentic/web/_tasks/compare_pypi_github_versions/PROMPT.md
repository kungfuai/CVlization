This task has three steps. Do them in order.

Step 1. Open https://pypi.org/project/pytest/. At the top of the page there is the latest stable version number of pytest, formatted like '8.x.y'. Note it.

Step 2. In a new navigation, open https://github.com/pytest-dev/pytest/releases. At the top of the releases list is the most recent release. It has a tag name, typically formatted like 'pytest-8.x.y' (or just '8.x.y'). Note that version number too.

Step 3. Compare the two version numbers you noted. If their major.minor.patch parts are identical, reply with: match: <version>  (for example 'match: 8.4.3'). If they differ, reply with: mismatch: pypi=<a> github=<b>  (for example 'mismatch: pypi=8.4.3 github=8.4.2').

Reply with only that single line at the end, no commentary or formatting.
