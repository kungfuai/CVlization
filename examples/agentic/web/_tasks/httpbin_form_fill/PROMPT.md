Go to https://httpbin.org/forms/post. Fill these three text fields: customer name 'Alice Example', telephone '5551234567', email 'alice@example.com'. Leave all other fields (size, topping, delivery time, comments) at their defaults. Click the 'Submit order' button. After the form submits, the page will display a JSON response containing the submitted form data along with metadata. In that JSON, find the value of the 'url' field (it is the URL the form was submitted to, which is different from the page you were on). Reply with only that URL on a single line, no commentary or formatting.
