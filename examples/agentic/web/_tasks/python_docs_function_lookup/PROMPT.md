Open https://docs.python.org/3/library/os.html. This is the official documentation for Python's 'os' module. The page lists many functions. Find the function in the 'os' module that returns the current working directory as a string. Reply with only the function name on a single line, in the form 'os.<name>' (e.g. 'os.something'). No commentary, no parentheses, no arguments, no formatting.
