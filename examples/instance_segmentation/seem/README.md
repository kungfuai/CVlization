## Quickstart

```
bash examples/instance_segmentation/seem/build.sh
bash examples/instance_segmentation/seem/predict.sh
```

The prediction script is currently just a sanity check. It will print out the shapes of output tensors.