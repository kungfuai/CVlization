dataset:
  path: "json"
  data_files: "$DOC_EXTRACTION_SFT_TRAIN_JSONL"
  split: "train"
  target_sources:
    - "ground_truth"
    - "form_identification"
  target_column: "target_json"
  split_strategy: "document_id"
  keep_only_clean_teacher_traces: false
  ground_truth_target_format: "value_only"
  transform_ground_truth_schema: true
  pretty_json: false

model:
  name: "unsloth/Llama-3.1-8B-Instruct-unsloth-bnb-4bit"
  load_in_4bit: true
  max_seq_length: 32768
  max_target_length: 8192

lora:
  r: 32
  alpha: 64
  dropout: 0.0
  use_gradient_checkpointing: "unsloth"
  target_modules:
    - "q_proj"
    - "k_proj"
    - "v_proj"
    - "o_proj"
    - "gate_proj"
    - "up_proj"
    - "down_proj"

training:
  output_dir: "./outputs/unsloth-llama31-8b-document-extraction-sft-multitask-32k-600"
  max_steps: 600
  num_epochs: 3
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 8
  learning_rate: 1.0e-4
  warmup_steps: 25
  lr_scheduler_type: "cosine"
  optim: "adamw_8bit"
  weight_decay: 0.01
  logging_steps: 1
  save_steps: 100
  save_total_limit: 4
  do_eval: false
  eval_split_ratio: 0.1
  eval_steps: 100
  seed: 3407
