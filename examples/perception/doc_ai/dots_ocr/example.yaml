name: dots_ocr
capability: perception/doc_ai
modalities:
- vision
- text
datasets:
- custom
stability: stable
resources:
  gpu: 1
  vram_gb: 8
  disk_gb: 5
presets:
  build:
    script: build.sh
    description: Build the Docker image
  predict:
    script: predict.sh
    description: Run predict
    path_args:
      - flag: "--image"
        type: file
      - flag: "--model-path"
        type: file
      - flag: "--output"
        type: file
tags:
- ocr
- document-understanding
- pytorch
tasks:
- ocr
frameworks:
- pytorch
description: Document OCR using DOTS (Document-Oriented Text Spotting)
verification:
  last_verified: "2025-12-30"
  last_verification_note: "Verified build and predict with bundled sample (invoice image) on Blackwell RTX PRO 6000. Upgraded to PyTorch 2.9.1 + CUDA 12.8. Fixed bundled sample handling. Outputs structured JSON with bounding boxes, categories, and extracted text/tables."
