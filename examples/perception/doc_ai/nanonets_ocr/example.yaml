name: nanonets_ocr
capability: perception/doc_ai
modalities:
- vision
- text
datasets:
- custom
stability: stable
resources:
  gpu: 1
  vram_gb: 8
  disk_gb: 5
presets:
  build:
    script: build.sh
    description: Build the Docker image
  predict:
    script: predict.sh
    description: Run predict
    path_args:
      - flag: "--input"
        type: file
      - flag: "--output"
        type: file
tags:
- ocr
- nanonets
- api
tasks:
- ocr
frameworks:
- api
description: Document OCR using Nanonets-OCR2-3B VLM model (qwen2_5_vl architecture)

verification:
  last_verified: "2025-12-30"
  last_verification_note: "Verified on Blackwell RTX PRO 6000. Upgraded to PyTorch 2.9.1 + CUDA 12.8 runtime. Pinned transformers==4.55.4 (model's original version). Fixed bundled sample handling. VRAM: ~8GB."
