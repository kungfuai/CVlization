name: instance-segmentation-torchvision
docker: torchvision_is
capability: perception/segmentation
modalities:
- vision
datasets:
- penn-fudan-pedestrian
stability: stable
resources:
  gpu: 1
  vram_gb: 12
  disk_gb: 10
presets:
  build:
    script: build.sh
    description: Build the Docker image
  train:
    script: train.sh
    description: Run train
tags:
- torchvision
- instance-segmentation
- mask-rcnn
- pytorch
tasks:
- instance_segmentation
frameworks:
- pytorch
- torchvision
description: Instance segmentation training with Torchvision (Mask R-CNN)
verification:
  last_verified: "2025-12-31"
  last_verification_note: "Upgraded to PyTorch 2.9.1+CUDA 12.8. Mask R-CNN training on Penn-Fudan. mAP 70%, mAP@50 99.8% after 5 epochs. VRAM: ~12GB."
