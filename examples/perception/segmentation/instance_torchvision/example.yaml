name: instance-segmentation-torchvision
docker: torchvision_is
capability: perception/segmentation
modalities:
- vision
datasets:
- penn-fudan-pedestrian
stability: stable
resources:
  gpu: 1
  vram_gb: 12
  disk_gb: 10
presets:
  build:
    script: build.sh
    description: Build the Docker image
  train:
    script: train.sh
    description: Run train
tags:
- torchvision
- instance-segmentation
- mask-rcnn
- pytorch
tasks:
- instance_segmentation
frameworks:
- pytorch
- torchvision
description: Instance segmentation training with Torchvision (Mask R-CNN)
verification:
  last_verified: 2025-10-26
  last_verification_note: "Build successful (PyTorch 2.4.0, Lightning 2.4.0). Training verified with Mask R-CNN on Penn-Fudan pedestrian dataset. Model quality clearly improving with mAP 0.05% → 99%."
