name: panoptic-segmentation-torchvision
capability: perception/segmentation
modalities:
- vision
datasets:
- coco-panoptic
stability: stable
resources:
  gpu: 1
  vram_gb: 16
  disk_gb: 15
presets:
  build:
    script: build.sh
    description: Build the Docker image
  train:
    script: train.sh
    description: Run train
tags:
- torchvision
- panoptic-segmentation
- pytorch
tasks:
- panoptic_segmentation
frameworks:
- pytorch
- torchvision
description: Panoptic segmentation training with Torchvision
verification:
  last_verified: 2025-10-26
  last_verification_note: "Build successful (PyTorch 2.4.0, Lightning 2.4.0). Fixed dtype conversion bug in validation metrics computation. Training verified with FCOS detector on Kitti-tiny dataset. Training runs successfully through multiple epochs with validation metrics: mAP 8.4%, mAP@50 22.4%."
