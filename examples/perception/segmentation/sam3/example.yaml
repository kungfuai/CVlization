name: sam3
capability: perception/segmentation
modalities:
  - vision
  - text
stability: experimental
resources:
  gpu: 1
  vram_gb: 16
  disk_gb: 15
image: sam3
presets:
  build:
    script: build.sh
    description: Build the SAM3 inference image
  predict:
    script: predict.sh
    description: Run text-prompted SAM3 segmentation on an image
tags:
  - sam3
  - segmentation
  - inference
  - open-vocabulary
frameworks:
  - pytorch
  - transformers
description: SAM3 text-prompted segmentation inference in a slim runtime container.
tasks:
  - instance_segmentation
  - semantic_segmentation
verification:
  last_verified: "2025-12-30"
  last_verification_note: "Verified on Blackwell RTX PRO 6000. Upgraded to PyTorch 2.9.1+CUDA 12.8 runtime. Uses transformers from git (PyPI 4.57.3 doesn't include Sam3Model). Lowered default score-threshold to 0.1. Requires HF authentication for gated model. VRAM: ~10GB."
