name: sam
capability: perception/segmentation
image: sam_is
modalities:
- vision
datasets:
- custom
stability: experimental
resources:
  gpu: 1
  vram_gb: 16
  disk_gb: 10
presets:
  build:
    script: build.sh
    description: Build the Docker image
  train:
    script: train.sh
    description: Run SAM training
tags:
- sam
- segment-anything
- meta
- pytorch
- mobile-sam
tasks:
- instance_segmentation
frameworks:
- pytorch
description: Fine-tune Meta's Segment Anything Model (SAM) using MobileSAM for instance segmentation on custom datasets
verification:
  last_verified: "2026-01-01"
  last_verification_note: "PyTorch 2.9.1+CUDA 12.8. Build verified, training starts on PennFudan dataset."
