name: sam3-finetuning
capability: perception/segmentation
modalities:
  - vision
  - text
stability: experimental
resources:
  gpu: 1
  vram_gb: 24
  disk_gb: 30
image: sam3-finetuning
presets:
  build:
    script: build.sh
    description: Build the SAM3 finetune image
  train:
    script: train.sh
    description: Train SAM3 on COCO-format segmentation dataset
tags:
  - sam3
  - segmentation
  - finetune
  - open-vocabulary
frameworks:
  - pytorch
  - transformers
datasets:
  - coco
description: SAM3 (Segment Anything Model 3) fine-tuning on custom segmentation datasets with COCO annotations
tasks:
  - instance_segmentation
  - semantic_segmentation
verification:
  last_verified: 2026-02-11
  last_verification_note: "Verified build, 2-epoch training on keremberke/pcb-defect-segmentation HF dataset. Loss decreasing (117→95), val mAP@0.50=0.25, checkpoint saved. 17GB VRAM on RTX PRO 6000 (98GB). Fixed --epochs override, checkpoint saving, and NaN matcher crash."
