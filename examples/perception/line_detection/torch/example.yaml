name: line-detection-torch
capability: perception/line_detection
image: line_detection_torch
modalities:
- vision
datasets:
- custom
stability: stable
resources:
  gpu: 1
  vram_gb: 6
  disk_gb: 5
presets:
  build:
    script: build.sh
    description: Build the Docker image
  train:
    script: train.sh
    description: Run train
tags:
- line-detection
- pytorch
tasks:
- line_detection
frameworks:
- pytorch
description: Line detection in images using PyTorch
verification:
  last_verified: "2026-01-01"
  last_verification_note: "PyTorch 2.9.1+CUDA 12.8. Training verified on Blackwell (loss ~30→22 over 2 epochs)."
