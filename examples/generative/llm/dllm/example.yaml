name: dllm
capability: generative/llm
modalities:
  - text
datasets: []
stability: experimental
resources:
  gpu: 1
  vram_gb: 2  # Minimum for qwen-bd3lm; llada needs 16GB
  disk_gb: 20
image: cvlization/dllm:latest
presets:
  build:
    script: build.sh
    description: Build the Docker image
  predict:
    script: predict.sh
    description: Run diffusion inference (default qwen-bd3lm)
  test:
    script: test.sh
    description: Run smoke test
tags:
  - dllm
  - diffusion
  - bd3lm
  - mdlm
  - llada
  - bert
  - qwen
  - inference
  - pytorch
tasks:
  - text_generation
frameworks:
  - pytorch
  - transformers
  - dllm
description: Diffusion Language Models (dLLM) - text generation via iterative denoising. Supports Qwen-BD3LM (0.6B), BERT-Chat (395M), and LLaDA (8B).
verification:
  last_verified: "2026-01-01"
  last_verification_note: "PyTorch 2.9.1+CUDA 12.8. Verified qwen-bd3lm inference ('What is 2+2?' -> '2+2 equals 4.') with 128 steps."
