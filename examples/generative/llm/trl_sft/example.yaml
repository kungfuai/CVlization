name: trl-llama-3b-sft
capability: generative/llm
modalities:
- text
datasets:
- yahma/alpaca-cleaned
stability: stable
resources:
  gpu: 1
  vram_gb: 8
  disk_gb: 10
image: trl_sft
presets:
  build:
    script: build.sh
    description: Build the Docker image
  train:
    script: train.sh
    description: Run train
tags:
- trl
- llama
- fine-tuning
- lora
- sft
- pytorch
tasks:
- text_generation
frameworks:
- pytorch
- trl
description: Fine-tune Llama 3.2 3B with HuggingFace TRL (supervised fine-tuning with
  LoRA)
verification:
  last_verified: "2026-01-01"
  last_verification_note: "PyTorch 2.9.1+CUDA 12.8. TRL 0.16.0 SFT verified with Qwen 3B + LoRA + 4-bit quant. 100 steps in ~63s. Loss: 1.55→1.18."
