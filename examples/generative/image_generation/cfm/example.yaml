name: cfm
capability: generative/image_generation
modalities:
- vision
datasets:
- custom
stability: beta
resources:
  gpu: 1
  vram_gb: 16
  disk_gb: 10
presets:
  build:
    script: build.sh
    description: Build the Docker image
  train:
    script: train.sh
    description: Run train
tags:
- conditional-flow-matching
- diffusion
- pytorch
tasks:
- image_generation
frameworks:
- pytorch
description: Conditional Flow Matching for image generation
verification:
  last_verified: "2026-01-01"
  last_verification_note: "PyTorch 2.9.1+CUDA 12.8. Training verified with CIFAR10 (170MB download). Loss 1.20→1.16 over 100 steps. 34.09M params, ~8 it/s."
