name: vqgan
capability: generative/image_generation
modalities:
- vision
datasets:
- custom
stability: stable
resources:
  gpu: 1
  vram_gb: 16
  disk_gb: 20
presets:
  build:
    script: build.sh
    description: Build the Docker image
  train:
    script: train.sh
    description: Run train
tags:
- vqgan
- vector-quantization
- gan
- pytorch
tasks:
- image_generation
frameworks:
- pytorch
description: VQGAN (Vector Quantized GAN) for high-quality image generation
verification:
  last_verified: 2025-10-25
  last_verification_note: "Build successful. Training initialization, dataset loading (HuggingFace MNIST), and metrics logging verified on A10 GPU (24GB VRAM)."
