name: edm2
capability: generative/image_generation
modalities:
- vision
datasets:
- custom
stability: beta
resources:
  gpu: 1
  vram_gb: 16
  disk_gb: 20
presets:
  build:
    script: build.sh
    description: Build the Docker image
  train:
    script: train.sh
    description: Run train
tags:
- edm2
- diffusion
- nvidia
- pytorch
tasks:
- image_generation
frameworks:
- pytorch
description: EDM2 (Elucidating Diffusion Models 2) from NVIDIA
verification:
  last_verified: 2025-10-26
  last_verification_note: "Verified build successful (22.2GB Docker image), train script initialization, and --help output. Fixed train.sh to call train_edm2.py. Removed non-existent predict preset. Requires external datasets in ImageFolder format (e.g., ImageNet). Tested on NVIDIA A10 (24GB VRAM)."
