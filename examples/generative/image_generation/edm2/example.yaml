name: edm2
capability: generative/image_generation
modalities:
- vision
datasets:
- custom
stability: beta
resources:
  gpu: 1
  vram_gb: 16
  disk_gb: 20
presets:
  build:
    script: build.sh
    description: Build the Docker image
  train:
    script: train.sh
    description: Run train
tags:
- edm2
- diffusion
- nvidia
- pytorch
tasks:
- image_generation
frameworks:
- pytorch
description: EDM2 (Elucidating Diffusion Models 2) from NVIDIA
verification:
  last_verified: "2026-01-01"
  last_verification_note: "PyTorch 2.9.1+CUDA 12.8. Train script --help verified. Requires ImageFolder datasets (e.g., ImageNet)."
