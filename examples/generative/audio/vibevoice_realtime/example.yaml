name: vibevoice_realtime
capability: generative/audio
modalities:
  - text
  - audio
datasets:
  - custom
stability: experimental
resources:
  gpu: 1
  vram_gb: 8
  disk_gb: 15
presets:
  build:
    script: build.sh
    description: Build the Docker image
  predict:
    script: predict.sh
    description: Generate speech from text
    path_args:
      - flag: "--input"
        type: file
      - flag: "--output"
        type: file
  test:
    script: test.sh
    description: Run smoke test
tags:
  - tts
  - text-to-speech
  - speech-synthesis
  - real-time
  - streaming
  - microsoft
  - qwen
tasks:
  - text_to_speech
  - speech_synthesis
frameworks:
  - pytorch
  - transformers
description: Real-time text-to-speech with ~300ms latency using Microsoft VibeVoice-Realtime-0.5B

verification:
  last_verified: "2025-12-30"
  last_verification_note: "Upgraded to PyTorch 2.9.1+CUDA 12.8 for Blackwell. Uses eager attention (flash-attn fails on runtime image). VRAM: ~8GB."
